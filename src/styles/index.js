import { createMuiTheme } from "@material-ui/core"

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#659DBD",
    },
    secondary: {
      main: "#DAAD89",
    },
    logo: {
      100: "#8CB6",
    },
  },
});

export default theme;
